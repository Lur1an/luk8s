apiVersion: v1
kind: Service
metadata:
  name: {{ include "deez.fullname" . }}
  labels:
    {{- include "deez.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deez.selectorLabels" . | nindent 4 }}
