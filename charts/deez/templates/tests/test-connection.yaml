apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deez.fullname" . }}-test-connection"
  labels:
    {{- include "deez.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deez.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
