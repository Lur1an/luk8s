apiVersion: v1
kind: Service
metadata:
  name: {{ include "deez1.fullname" . }}
  labels:
    {{- include "deez1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deez1.selectorLabels" . | nindent 4 }}
